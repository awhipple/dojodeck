# dojodeck manifest — one game per line:  <slug> <git-url>
# `tools/publish <slug>` (on the dev box) appends a line here and pushes.
# dojodeck clones/pulls each of these on launch. SSH URLs use the Deck's key.
#
# example:
#   asteroids git@github.com:awhipple/asteroids.git
