# dojodeck manifest — one game per line:  <slug> <git-url>
# `tools/publish <slug>` (on the dev box) appends a line here and pushes.
# dojodeck clones/pulls each of these on launch (public repos, anonymous HTTPS).
#
# example:
#   asteroids https://github.com/awhipple/asteroids.git
